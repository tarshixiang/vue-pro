import * as types from '../types'

const state = {
    products : [1,2]
}

const getters = {
    products : state => state.products
}

const actions = {
   
}
const mutations = {

}
export default {
  state,
  getters,
  actions,
  mutations
}
