import * as types from '../types'

const state = {
    cart : []
}

const getters = {
    cart : state => {state.cart}
}

const actions = {
   
}
const mutations = {

}
export default {
  state,
  getters,
  actions,
  mutations
}
